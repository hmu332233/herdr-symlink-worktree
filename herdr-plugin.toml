id = "dev.minung.symlink-worktree"
name = "Symlink Worktree"
version = "0.1.0"
min_herdr_version = "0.7.0"
description = "Symlink shared local files from your main repo into new worktrees."
platforms = ["linux", "macos"]

[[events]]
on = "worktree.created"
command = ["bash", "src/link.sh"]
